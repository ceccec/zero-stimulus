name: Deploy Zero-Stimulus PWA

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      # Add your deploy step here, e.g. GitHub Pages, Netlify, etc.
      - name: Deploy (placeholder)
        run: echo "Deploy step goes here" 